# WEEK-12 — Acceptance: master scenario + release checklist + v1 ship

> Exit test: the contract §215 master acceptance scenario passes end-to-end (all 35 steps), the §207 release checklist is fully ticked, and v1 is tagged with artifacts attached.

## Scope

- Run §215 verbatim and record evidence for every step (this becomes the release report):
  install both sides → pair → Puter AI + OpenAI BYOK both configured → switch providers → GitHub connect → work feed with issue + AI-opportunity-with-evidence + reference → swipe left (preference updates) → swipe right → contract → OpenCode start → isolated worktree verified → live events on Android → phone disconnect (PC continues) → reconnect (replay verified) → follow-up into SAME session → protected action → approval appears → deny once (action not executed) → re-ask → approve exact action → policy authorizes → GitHub branch/PR operation → audit event exists → no secrets in logs → workstation restart → session/task reconciliation → localhost UI after restart.
- §207 release checklist run on real clean machines, not assumptions.
- PROJECT_STATUS.md final pass: every label must be true.
- Tag v1.0.0, publish release artifacts + changelog.
- Post-v1 backlog promotion: SUGGESTIONS.md items the user approves become week files (Harness adapter, Tailscale transport, shared discovery index via Puter Workers, portfolio mode, relay service).

## Notes

- If any step fails, the system is not finished (contract §215). Fix and re-run the whole scenario — no partial credit.
