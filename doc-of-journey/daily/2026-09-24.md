# 2026-09-24 — WEEK-00: foundation session (repo born)

## Implementation audit (contract §212, required first response)

- **Existing architecture**: none. `rest-things/` (outside the repo) is academic spec material — SRS, UML class diagrams, approval-workflow PDFs, screenshots. No source tree, no configs, no tests.
- **Existing source coverage**: 0%. Greenfield build.
- **Missing systems**: everything — providers, GitHub, discovery, ranking, opportunity engine, workstation, agent gateway, policy, approvals, UIs, packaging, CI.
- **Proposed final topology**: per `docs/ARCHITECTURE.md` (local-first control plane on the workstation daemon; Puter optional client-side; OpenCode behind our AgentGateway; one React app wrapped by Capacitor for Android).
- **Major security risks discovered**: none in code (no code); the risk list is the contract's threat model (§49) — top items: malicious repo content / prompt injection, secret exposure, worktree escape, approval replay. All have planned enforcement points (WEEK-06/07/10).
- **Technology choices retained**: contract §181 defaults — React+TS+Vite, Capacitor, provider-agnostic AI, OpenCode-first.
- **Technology choices changed**: none materially. Toolchain additions: npm workspaces (boring, installed), zod (contract §189 requires boundary validation), vitest, tsx for dev-run. PostgreSQL/FastAPI from the early session draft deliberately NOT carried over (contract §181 forbids infra for fashion).
- **Legacy material retained** (contract §39): UML concepts (User/AIConfiguration/GitHubItem/OpenCodeEnvironment/ManagerObjective/AutomationLog/ExecutionSession/AgentTask/TerminalLog/ApprovalRequest) mapped into `@jarvis/protocol` domain contracts. `reasoning_path` from the old UML = execution-plan/decision-summary ONLY (no chain-of-thought), per session decision.

## User directives captured this session

1. Git repo: `https://github.com/arcgod-design/gitswipe.git`, branch `main`.
2. License: none yet — deferred until user picks; no public release before it.
3. Android: Capacitor (Android Studio present on the user's PC).
4. Session scope: foundation + as far as possible + full weeks ladder so any future session resumes with zero hallucinations.
5. Frontend work always uses the taste-skill pack + ui-ux-pro-max (locked PROJECT-CORE #15).
6. Dispatched tasks follow the ssoc git work pattern (locked PROJECT-CORE #18, ADR 0004, `docs/REPO-WORK-CONVENTIONS.md`).

## What was built today

- Full documentation system (Astro/lattice pattern) — see repo root.
- `packages/protocol`: ids, event envelope, session + task state machines, TaskContract v1 (+ markdown render + git_workflow block), opportunities/swipes/evidence, approvals + canonical-JSON action hashing, policy types, AgentGateway interfaces, workstation handshake, failure categories.
- `packages/policy`: deterministic rule engine (priority + explainability + unmatched→APPROVAL_REQUIRED), command classification per contract §26, path sandbox (traversal/blocked/windows), table-driven tests per §118.
- `packages/providers`: AIProvider abstraction, OpenAI-compatible adapter (chat/stream/models/health), Anthropic adapter, registry presets (openai/openrouter/ollama/gemini-compat/generic), routing, 429 retry-once, error→category mapping, Puter client-bridge PROTO. Tests run against local mock HTTP servers — zero live keys.
- `apps/daemon`: CLI (`--version`, `--check`, `--serve` = honest not-implemented).
- CI workflow, `.env.example`, weeks/WEEK-00..12, memory protocol, ADRs 0001–0004.

## Verification (exact commands + results — recorded after the run; see SESSION-STATE.md for final state)

- `npm install` — workspace links created.
- `npm run typecheck` — all packages strict-clean.
- `npm test` — vitest suite green (protocol + policy + providers).
- `npm run daemon -- --check` — preflight-lite passes.

## Open threads

- Product name: contract says "Jarvis", repo is "gitsweep" — user to confirm (USER-THING-TO-DO #1).
- GitHub App vs OAuth (WEEK-02), Puter creds (WEEK-08), license (before any public release).
