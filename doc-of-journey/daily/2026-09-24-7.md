# 2026-09-24 (session 7) — MVP M1: demo server (branch: mvp)

## What was built

- `apps/daemon/src/demo-data.ts` — 3 fictional repos + 6 fixture issues (fresh/stale, 3 languages, welcoming labels), DEMO notice, ssoc commit identity, isolated demo worktree root.
- `apps/daemon/src/demo-server.ts` — `startDemoServer`: loopback-only node:http server, per-start Bearer token, `x-demo-mode: true` on every response; routes over the REAL engines:
  - `GET /healthz`
  - `GET /api/feed` — fixtures through `rankFeed` + `buildFeedPage` (swipe-aware, graph persisted across requests)
  - `POST /api/swipe` — zod-validated §9.3 actions; appends to `JsonlSwipeStore` (demo data dir), applies to skill graph, returns the updated feed
  - `POST /api/task` — real `TaskContractSchema.parse` + `toMarkdown` with the ssoc git_workflow block (feat/issue-N-slug branch, origin/upstream remotes, prepush gates)
- `jarvisd demo [--port=N]` command (Ctrl+C-safe shutdown).
- 8 integration tests: token enforcement (401), demo notice + badge, ranked explainable feed, STALE_ISSUE risk reasons, swipe→feed-change through HTTP, invalid action 400, unknown key 404, contract + markdown shape.

## Bug caught

- `startDemoServer` resolved with the REQUESTED port (0) instead of the ASSIGNED one (`server.address().port`) — every fetch hit `127.0.0.1:0` -> EADDRNOTAVAIL. Fixed; noted for the WEEK-05 daemon too.

## Verification (exact commands)

- `npm run typecheck` -> 5/5 clean
- `npm test` -> 124/124 (14 files; daemon suite 8/8)
- Live run: `npx tsx apps/daemon/src/index.ts demo --port=7421` -> banner + token; `GET /healthz` -> {"ok":true,"demo":true}; `GET /api/feed` with token -> ranked card (skill_match 1.0, clarity 0.8, score 0.84); unauthenticated -> 401.

## Next

M2 on mvp: JSONL event journal with replay-from-cursor, SSE endpoint, deterministic mock agent (contract s202 script: inspect -> file changes -> tests -> approval request -> complete) driving a session endpoint.