# 2026-09-24 (session 9) — MVP M3: the demo UI (branch: mvp) — MVP TRACK COMPLETE

## Design pass (mandatory per PROJECT-CORE #15/#23)

- ui-ux-pro-max design-system run for "developer tool agent control plane dashboard dark tech terminal" -> slate-900 dark base (#0F172A/#1E293B), green run-accent #22C55E ("code dark + run green"), Space Grotesk display + DM Sans body + JetBrains Mono for all data. 200-300ms transitions. Anti-patterns noted: flat-no-depth, text-heavy.
- design-taste-frontend discipline applied: dials V4/M4/D6 (product app, not landing); one accent locked (green; warn/danger are semantic states only); radius lock (16/10/8); no Inter default; no pure black/white; no emoji icons (@phosphor-icons/react, one family, consistent weight); copy audited (all functional, mock agent labeled honestly); event stream honestly labeled "Event stream (live)" per ADR 0008 (no fake terminal); reduced-motion gated everywhere; keyboard swipe per contract s185; color contrast checked (muted 6.2:1, accent-ink on green, warn-ink on warn).

## What was built

- `apps/web` (React 18 + Vite + TS): App state machine (feed -> contract -> session), three screens, shared design tokens stylesheet (styles.css), typed API client with token-from-URL (sessionStorage, stripped from history), shared SSE parser (`src/sse.ts`, unit-tested, reused by the fetch stream client).
- Daemon static serving: GET / serves the built UI from apps/daemon/public (webRoot anchored to import.meta.url, cwd-independent), traversal-safe (resolve + startsWith), correct MIME types, honest 404 when the UI is not built yet ("run npm run build -w @jarvis/web").
- Root scripts: `npm run demo` (starts the pitch demo), `npm run build:web`.

## Bugs caught

- JSX literal-string bug in my own code (className="state-badge ${...}" - not a template literal) - caught on read-through before shipping.
- `moduleResolution: NodeNext` cannot resolve @phosphor-icons/react's exports map -> web tsconfig uses Bunder resolution (correct for a Vite app anyway).
- `payload.branch` is unknown (Record<string, unknown>) -> String() coercion in JSX.

## Verification

- `npm run typecheck` -> 6/6 workspaces clean (web included)
- `npm test` -> 132/132 (17 files; web SSE parser 3 tests)
- `npm run build:web` -> built cleanly to apps/daemon/public (fonts bundled, 54KB gzip JS)
- Live: GET / serves index.html (200 text/html); ../ and ..%2f traversal -> 404; API feed healthy with token (4 cards, real engine output)
- Visual browser confirmation is the user's first `npm run demo` run (no headless browser in this session).

## Status

**MVP TRACK COMPLETE.** Pitch run: npm install && npm run build:web && npm run demo -> open printed URL.