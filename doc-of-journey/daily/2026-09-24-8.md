# 2026-09-24 (session 8) — MVP M2+M4 complete; ADR 0008 (harness connection) (branch: mvp)

## What was built

**M2 — journal + SSE + mock agent:**
- `apps/daemon/src/journal.ts` — SessionJournal: append-only JSONL, monotonic sequence with gap check, readFrom(cursor) replay.
- `apps/daemon/src/mock-agent.ts` — deterministic agent script (contract s202): TaskQueued -> PreflightStarted -> PreflightPassed -> AgentStarted -> ThinkingSummary -> FileChanged x2 -> PatchGenerated -> TestStarted -> TestFinished -> ApprovalRequested (real makeApprovalRequest with action-hash binding) -> [human gate] -> ApprovalGranted -> CommitCreated -> TestStarted -> TestFinished -> PRCreated (REVIEW_READY) -> AgentCompleted. All transitions validated against the protocol state machine.
- demo-server: POST /api/session, GET /api/session/:id, GET /api/session/:id/events (SSE, replay-from-cursor + live tail + done on terminal state), POST /api/session/:id/approve (verifies the real approval binding; deny -> PAUSED; 409 when nothing pending).
**M4:** POST /api/demo/reset (wipes demo swipes, restores feed; demo-dir only).

## ADR 0008 — harness connection verdict (user asked: munder-difflin vs Hermes plugin vs own logic)

Studied munder-difflin (7.9k stars, TS Electron): viewer/controller over tmux-paned Claude Code agents; dual-plane = hooks-JSON event plane + tmux raw terminal plane. **Our AgentGateway stays** (attach model can't drive dispatch; OpenCode has a real API so no hooks patching needed). **Adopted ideas:** dual-plane separation (event journal vs raw terminal channel — WEEK-06 must add the real terminal plane) + takeover via attachable session (tmux on unix; OpenCode session-continue on Windows). No code, no dependencies.

## Bugs the tests caught this session (all real spec violations)

1. Mock agent jumped CREATED -> RUNNING; state machine demanded the full chain. Fixed: agent now walks QUEUED -> PREFLIGHT -> STARTING -> RUNNING (and the demo now SHOWS preflight).
2. Mock agent finished RUNNING -> COMPLETED, skipping TESTING -> REVIEW_READY. Fixed: post-approval formal verification pass added (commit -> test -> PR draft -> review-ready -> complete).
3. Test harness: SSE reader pump never started in two tests + `done` boolean snapshotted at construction (getter fixed). Server itself was verified correct via raw live dump first.

## Verification

- `npm run typecheck` -> 5/5 clean
- `npm test` -> 129/129 (daemon suite 13/13)
- Live: session status shows state WAITING_FOR_APPROVAL with 11 journaled events; raw SSE dump shows events + `event: done` terminator.

## Next

M3 (taste-skill + ui-ux-pro-max design pass -> the four screens) — needs a fresh session with the design skills loaded. Then mvp is pitchable end-to-end.