# 2026-09-24 (session 6) — branch model live: dev / main / mvp

## What happened

User directive: three branches — `mvp` (pure MVP for pitch/examples), `main` (release versions), `dev` (development; verified work merges to main). Implemented as **ADR 0007**:

- `dev` created at the WEEK-04 head; ALL ladder work + session pushes target dev from now on.
- `main` = releases only; first verified merge (dev → main, `--no-ff`) tagged **v0.1.0** (WEEK-00..04: 116/116 tests, typecheck 5/5, audit 0).
- `mvp` branched from dev for the pitch/demo slice; demo shortcuts must be labeled demo mode (contract §201); merges to dev only when production-grade.
- CI now runs on main, dev, and mvp.
- Updated: CLAUDE.md hard rules, memory-protocol.md (push to dev + milestone merge procedure), PROJECT-CORE decision #22.

## Verification

- Branches exist locally; pushed with upstream tracking (see state JSON).
- Post-branch suite: typecheck 5/5, tests green (run before the merge to main).

## Notes

- GitHub default branch stays `main` (outsiders see releases).
- Branch protection on main (no force-push/delete) attempted via gh; result recorded in SESSION-STATE.
